export interface Project {
  image: string;
  title: string;
  description: string[];
  tags: string[];
  liveUrl?: string;
  repoUrl?: string;
  company?: string;
  role?: string;
  duration?: string;
}